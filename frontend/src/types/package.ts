export interface Package {
    title: string;
    location: string;
    description: string;
    duration: string;
    price: number;
    image: string;
    features: string[];
    rating: number;
    reviewCount: number;
  }